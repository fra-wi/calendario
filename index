<!DOCTYPE html>
<html lang="it">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Calendario</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700&display=swap');

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg: #f6f7fb;
  --surface: #ffffff;
  --surface2: #f0f2f8;
  --border: #e4e6f0;
  --text: #1a1a2e;
  --muted: #8b8fa8;
  --accent: #3b5bfc;
  --accent-light: #eef0ff;
  --accent2: #f03c78;
  --today-bg: #3b5bfc;
  --today-text: #fff;
  --shadow: 0 1px 3px rgba(0,0,0,0.06), 0 4px 16px rgba(0,0,0,0.04);
  --shadow-md: 0 4px 24px rgba(59,91,252,0.12);
  --radius: 12px;
}

body { font-family: 'Plus Jakarta Sans', sans-serif; background: var(--bg); color: var(--text); height: 100vh; display: flex; flex-direction: column; overflow: hidden; }

.topbar { display: flex; align-items: center; gap: 14px; padding: 10px 18px; background: var(--surface); border-bottom: 1px solid var(--border); flex-shrink: 0; z-index: 10; }
.logo { font-size: 17px; font-weight: 700; color: var(--accent); letter-spacing: -0.03em; white-space: nowrap; }
.today-btn { background: var(--surface); border: 1.5px solid var(--border); color: var(--text); font-family: inherit; font-size: 12px; font-weight: 600; padding: 6px 14px; border-radius: 8px; cursor: pointer; transition: all .15s; white-space: nowrap; }
.today-btn:hover { border-color: var(--accent); color: var(--accent); }
.nav-btns { display: flex; gap: 2px; }
.nav-btn { background: none; border: none; color: var(--muted); width: 30px; height: 30px; border-radius: 7px; cursor: pointer; display: flex; align-items: center; justify-content: center; font-size: 15px; transition: all .15s; }
.nav-btn:hover { background: var(--surface2); color: var(--text); }
.current-label { font-size: 15px; font-weight: 700; letter-spacing: -0.02em; color: var(--text); min-width: 160px; }
.spacer { flex: 1; }
.view-switcher { display: flex; background: var(--surface2); border-radius: 9px; padding: 3px; gap: 2px; }
.view-btn { background: none; border: none; font-family: inherit; font-size: 12px; font-weight: 600; color: var(--muted); padding: 5px 12px; border-radius: 7px; cursor: pointer; transition: all .15s; white-space: nowrap; }
.view-btn.active { background: var(--surface); color: var(--accent); box-shadow: 0 1px 4px rgba(0,0,0,0.08); }

.chat-toggle-btn { background: var(--accent); border: none; color: #fff; font-family: inherit; font-size: 12px; font-weight: 700; padding: 7px 14px; border-radius: 8px; cursor: pointer; display: flex; align-items: center; gap: 6px; transition: opacity .15s; white-space: nowrap; }
.chat-toggle-btn:hover { opacity: .88; }
.chat-toggle-btn svg { width: 14px; height: 14px; flex-shrink: 0; }

.main { display: flex; flex: 1; overflow: hidden; }

.sidebar { width: 200px; flex-shrink: 0; background: var(--surface); border-right: 1px solid var(--border); padding: 14px 10px; overflow-y: auto; }
.mini-cal-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 8px; }
.mini-cal-title { font-size: 12px; font-weight: 700; color: var(--text); }
.mini-nav { display: flex; gap: 2px; }
.mini-nav button { background: none; border: none; color: var(--muted); width: 22px; height: 22px; border-radius: 5px; cursor: pointer; font-size: 11px; display: flex; align-items: center; justify-content: center; transition: all .15s; }
.mini-nav button:hover { background: var(--surface2); color: var(--text); }
.mini-grid { display: grid; grid-template-columns: repeat(7, 1fr); gap: 1px; }
.mini-day-name { font-size: 8px; font-weight: 700; color: var(--muted); text-align: center; padding: 3px 0; text-transform: uppercase; }
.mini-day { font-size: 10px; font-weight: 500; text-align: center; padding: 3px 1px; border-radius: 50%; cursor: pointer; transition: all .15s; color: var(--text); aspect-ratio: 1; display: flex; align-items: center; justify-content: center; }
.mini-day:hover { background: var(--surface2); }
.mini-day.other-month { color: var(--border); }
.mini-day.is-today { background: var(--today-bg); color: var(--today-text); font-weight: 700; }
.mini-day.in-view-week { background: var(--accent-light); color: var(--accent); }
.mini-day.is-today { background: var(--today-bg); color: var(--today-text); }

.cal-content { flex: 1; display: flex; flex-direction: column; overflow: hidden; background: var(--bg); }

.week-view { display: flex; flex-direction: column; flex: 1; overflow: hidden; background: var(--surface); margin: 10px; border-radius: var(--radius); border: 1px solid var(--border); box-shadow: var(--shadow); }
.week-header { display: grid; grid-template-columns: 52px repeat(7, 1fr); border-bottom: 1px solid var(--border); flex-shrink: 0; background: var(--surface); border-radius: var(--radius) var(--radius) 0 0; overflow: hidden; }
.week-header-gutter { border-right: 1px solid var(--border); }
.week-header-day { display: flex; flex-direction: column; align-items: center; padding: 8px 4px 6px; border-right: 1px solid var(--border); cursor: pointer; transition: background .15s; gap: 3px; }
.week-header-day:hover { background: var(--surface2); }
.week-header-day:last-child { border-right: none; }
.whd-name { font-size: 9px; font-weight: 700; color: var(--muted); text-transform: uppercase; letter-spacing: .08em; }
.whd-num { font-size: 18px; font-weight: 700; color: var(--text); width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; border-radius: 50%; transition: all .15s; }
.whd-num.is-today { background: var(--today-bg); color: var(--today-text); }
.whd-num.is-weekend { color: var(--muted); }
.week-body { display: flex; flex: 1; overflow-y: auto; }
.week-time-col { width: 52px; flex-shrink: 0; border-right: 1px solid var(--border); position: relative; }
.time-label { position: absolute; right: 7px; font-size: 8px; font-weight: 600; color: var(--muted); transform: translateY(-50%); white-space: nowrap; }
.week-days-grid { flex: 1; display: grid; grid-template-columns: repeat(7, 1fr); }
.week-day-col { border-right: 1px solid var(--border); position: relative; min-height: 1440px; }
.week-day-col:last-child { border-right: none; }
.week-day-col.is-weekend { background: rgba(0,0,0,.012); }
.hour-line { position: absolute; left: 0; right: 0; border-top: 1px solid var(--border); pointer-events: none; }
.hour-line.half { border-top-style: dashed; opacity: .5; }
.current-time-line { position: absolute; left: 0; right: 0; height: 2px; background: var(--accent2); z-index: 5; pointer-events: none; }
.current-time-line::before { content: ''; position: absolute; left: -4px; top: 50%; transform: translateY(-50%); width: 8px; height: 8px; border-radius: 50%; background: var(--accent2); }

.month-view { flex: 1; display: flex; flex-direction: column; margin: 10px; background: var(--surface); border-radius: var(--radius); border: 1px solid var(--border); box-shadow: var(--shadow); overflow: hidden; }
.month-header-row { display: grid; grid-template-columns: repeat(7, 1fr); border-bottom: 1px solid var(--border); }
.month-day-name { font-size: 9px; font-weight: 700; color: var(--muted); text-align: center; padding: 8px 4px; text-transform: uppercase; }
.month-grid { display: grid; grid-template-columns: repeat(7, 1fr); grid-template-rows: repeat(6, 1fr); flex: 1; overflow: hidden; }
.month-cell { border-right: 1px solid var(--border); border-bottom: 1px solid var(--border); padding: 5px 6px; display: flex; flex-direction: column; gap: 2px; cursor: pointer; transition: background .12s; overflow: hidden; min-height: 0; }
.month-cell:hover { background: var(--surface2); }
.month-cell:nth-child(7n) { border-right: none; }
.month-cell-num { font-size: 11px; font-weight: 700; color: var(--text); width: 22px; height: 22px; display: flex; align-items: center; justify-content: center; border-radius: 50%; flex-shrink: 0; }
.month-cell-num.other { color: var(--border); }
.month-cell-num.today { background: var(--today-bg); color: var(--today-text); }
.month-cell-num.is-weekend { color: var(--muted); }

.day-view { display: flex; flex-direction: column; flex: 1; overflow: hidden; background: var(--surface); margin: 10px; border-radius: var(--radius); border: 1px solid var(--border); box-shadow: var(--shadow); }
.day-view-header { display: flex; flex-direction: column; align-items: center; padding: 12px; border-bottom: 1px solid var(--border); flex-shrink: 0; }
.day-view-name { font-size: 10px; font-weight: 700; color: var(--muted); text-transform: uppercase; letter-spacing: .1em; }
.day-view-num { font-size: 28px; font-weight: 700; letter-spacing: -0.03em; }
.day-view-body { display: flex; flex: 1; overflow-y: auto; }
.day-time-col { width: 52px; flex-shrink: 0; border-right: 1px solid var(--border); position: relative; }
.day-col-content { flex: 1; position: relative; min-height: 1440px; }

.cal-event { position: absolute; left: 3px; right: 3px; border-radius: 6px; padding: 3px 6px; font-size: 10px; font-weight: 600; color: #fff; overflow: hidden; cursor: pointer; transition: filter .15s; z-index: 2; box-shadow: 0 1px 4px rgba(0,0,0,.15); }
.cal-event:hover { filter: brightness(1.08); }
.cal-event .ev-title { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.cal-event .ev-time { font-size: 8px; opacity: .8; font-weight: 400; }
.month-event { border-radius: 3px; padding: 1px 5px; font-size: 9px; font-weight: 600; color: #fff; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; cursor: pointer; }

.modal-overlay { position: fixed; inset: 0; background: rgba(26,26,46,.35); display: flex; align-items: center; justify-content: center; z-index: 100; backdrop-filter: blur(2px); opacity: 0; pointer-events: none; transition: opacity .2s; }
.modal-overlay.open { opacity: 1; pointer-events: all; }
.modal { background: var(--surface); border-radius: 16px; padding: 24px; width: 360px; box-shadow: 0 24px 64px rgba(0,0,0,.18); transform: translateY(12px); transition: transform .2s; }
.modal-overlay.open .modal { transform: translateY(0); }
.modal h3 { font-size: 16px; font-weight: 700; margin-bottom: 16px; }
.form-group { display: flex; flex-direction: column; gap: 4px; margin-bottom: 12px; }
.form-group label { font-size: 10px; font-weight: 700; color: var(--muted); text-transform: uppercase; letter-spacing: .08em; }
.form-group input, .form-group select, .form-group textarea { background: var(--surface2); border: 1.5px solid var(--border); border-radius: 8px; color: var(--text); font-family: inherit; font-size: 13px; padding: 8px 11px; outline: none; transition: border-color .15s; }
.form-group input:focus, .form-group select:focus, .form-group textarea:focus { border-color: var(--accent); }
.form-group textarea { resize: vertical; min-height: 52px; }
.color-picker { display: flex; gap: 7px; }
.color-dot { width: 24px; height: 24px; border-radius: 50%; cursor: pointer; border: 2.5px solid transparent; transition: transform .15s; }
.color-dot:hover { transform: scale(1.12); }
.color-dot.selected { border-color: var(--text); }
.time-row-form { display: flex; gap: 8px; align-items: center; }
.time-row-form span { color: var(--muted); font-size: 12px; }
.modal-actions { display: flex; gap: 8px; justify-content: flex-end; margin-top: 16px; }
.btn-cancel { background: none; border: 1.5px solid var(--border); color: var(--muted); font-family: inherit; font-size: 12px; font-weight: 600; padding: 8px 16px; border-radius: 8px; cursor: pointer; transition: all .15s; }
.btn-cancel:hover { border-color: var(--muted); color: var(--text); }
.btn-save { background: var(--accent); border: none; color: #fff; font-family: inherit; font-size: 12px; font-weight: 700; padding: 8px 20px; border-radius: 8px; cursor: pointer; transition: opacity .15s; }
.btn-save:hover { opacity: .88; }
.btn-delete { background: none; border: 1.5px solid #f03c78; color: #f03c78; font-family: inherit; font-size: 12px; font-weight: 600; padding: 8px 16px; border-radius: 8px; cursor: pointer; margin-right: auto; transition: all .15s; }
.btn-delete:hover { background: #f03c78; color: #fff; }

.fab { position: fixed; bottom: 22px; right: 22px; background: var(--accent); color: #fff; border: none; width: 48px; height: 48px; border-radius: 50%; font-size: 22px; cursor: pointer; box-shadow: var(--shadow-md); display: flex; align-items: center; justify-content: center; transition: transform .15s, opacity .15s; z-index: 50; }
.fab:hover { transform: scale(1.06); opacity: .92; }

/* ── CHAT PANEL ── */
.chat-panel { width: 0; flex-shrink: 0; background: var(--surface); border-left: 1px solid var(--border); display: flex; flex-direction: column; overflow: hidden; transition: width .25s cubic-bezier(.4,0,.2,1); }
.chat-panel.open { width: 300px; }
.chat-header { padding: 14px 16px 10px; border-bottom: 1px solid var(--border); flex-shrink: 0; }
.chat-header-top { display: flex; align-items: center; justify-content: space-between; margin-bottom: 3px; }
.chat-title { font-size: 14px; font-weight: 700; }
.chat-close { background: none; border: none; color: var(--muted); cursor: pointer; width: 26px; height: 26px; border-radius: 6px; font-size: 16px; display: flex; align-items: center; justify-content: center; transition: all .15s; }
.chat-close:hover { background: var(--surface2); color: var(--text); }
.chat-subtitle { font-size: 11px; color: var(--muted); line-height: 1.4; }
.chat-messages { flex: 1; overflow-y: auto; padding: 12px; display: flex; flex-direction: column; gap: 10px; }
.chat-msg { display: flex; gap: 8px; }
.chat-msg.user { flex-direction: row-reverse; }
.msg-bubble { padding: 9px 12px; border-radius: 12px; font-size: 12px; line-height: 1.5; max-width: 85%; word-break: break-word; }
.chat-msg.ai .msg-bubble { background: var(--surface2); color: var(--text); border-radius: 4px 12px 12px 12px; }
.chat-msg.user .msg-bubble { background: var(--accent); color: #fff; border-radius: 12px 4px 12px 12px; }
.msg-avatar { width: 26px; height: 26px; border-radius: 50%; flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-size: 13px; margin-top: 1px; }
.chat-msg.ai .msg-avatar { background: var(--accent-light); }
.chat-msg.user .msg-avatar { background: #e8eaff; }
.typing-dots span { display: inline-block; width: 5px; height: 5px; border-radius: 50%; background: var(--muted); margin: 0 1.5px; animation: bounce 1.2s infinite; }
.typing-dots span:nth-child(2) { animation-delay: .2s; }
.typing-dots span:nth-child(3) { animation-delay: .4s; }
@keyframes bounce { 0%,60%,100% { transform: translateY(0); } 30% { transform: translateY(-5px); } }
.chat-input-area { padding: 10px 12px 12px; border-top: 1px solid var(--border); flex-shrink: 0; }
.chat-input-row { display: flex; gap: 8px; align-items: flex-end; }
.chat-textarea { flex: 1; background: var(--surface2); border: 1.5px solid var(--border); border-radius: 10px; color: var(--text); font-family: inherit; font-size: 12px; padding: 9px 11px; outline: none; resize: none; transition: border-color .15s; min-height: 38px; max-height: 100px; line-height: 1.4; }
.chat-textarea:focus { border-color: var(--accent); }
.chat-send { background: var(--accent); border: none; color: #fff; width: 36px; height: 36px; border-radius: 9px; cursor: pointer; display: flex; align-items: center; justify-content: center; flex-shrink: 0; transition: opacity .15s; }
.chat-send:hover { opacity: .88; }
.chat-send svg { width: 14px; height: 14px; }
.chat-send:disabled { opacity: .4; cursor: not-allowed; }
.chat-hint { font-size: 10px; color: var(--muted); margin-top: 5px; }


/* ── MOBILE ── */
@media (max-width: 700px) {
  .sidebar { display: none; }
  .chat-panel.open { width: 100vw; position: fixed; top: 0; left: 0; right: 0; bottom: 0; z-index: 90; height: 100vh; }
  .topbar { padding: 8px 10px; gap: 8px; }
  .logo { font-size: 15px; }
  .current-label { font-size: 13px; min-width: 0; }
  .today-btn { padding: 5px 10px; font-size: 11px; }
  .view-btn { padding: 5px 8px; font-size: 11px; }
  .chat-toggle-btn { padding: 6px 10px; font-size: 11px; }
  .week-view { margin: 4px; border-radius: 8px; }
  .month-view { margin: 4px; border-radius: 8px; }
  .day-view { margin: 4px; border-radius: 8px; }
  .week-header-gutter { width: 36px; min-width: 36px; }
  .week-time-col { width: 36px; }
  .time-label { font-size: 7px; right: 3px; }
  .week-header { grid-template-columns: 36px repeat(7, 1fr); }
  .week-days-grid { grid-template-columns: repeat(7, 1fr); }
  .whd-name { font-size: 7px; letter-spacing: 0; }
  .whd-num { font-size: 13px; width: 24px; height: 24px; }
  .whd-holiday { font-size: 6px; padding: 1px 2px; }
  .week-header-day { padding: 5px 1px 4px; gap: 2px; }
  .cal-event { padding: 2px 3px; font-size: 8px; border-radius: 4px; }
  .cal-event .ev-time { display: none; }
  .modal { width: calc(100vw - 32px); padding: 18px; }
  .fab { bottom: 16px; right: 16px; width: 44px; height: 44px; font-size: 20px; }
  .month-cell-num { width: 18px; height: 18px; font-size: 10px; }
  .month-event { font-size: 8px; padding: 1px 3px; }
  .holiday-badge { font-size: 7px; padding: 1px 3px; }
}
::-webkit-scrollbar { width: 4px; } ::-webkit-scrollbar-track { background: transparent; } ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 99px; }

/* ── HOLIDAYS ── */
.holiday-badge { font-size: 8px; font-weight: 700; color: #c0392b; background: #fdecea; border-radius: 4px; padding: 1px 5px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; margin-top: 1px; }
.week-holiday-badge { font-size: 8px; font-weight: 700; color: #c0392b; background: #fdecea; border-radius: 4px; padding: 1px 4px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 100%; }
.day-holiday-banner { background: #fdecea; color: #c0392b; font-size: 11px; font-weight: 700; text-align: center; padding: 5px 12px; border-bottom: 1px solid #f5c6c6; }
.whd-holiday { font-size: 8px; font-weight: 700; color: #c0392b; background: #fdecea; border-radius: 4px; padding: 1px 4px; margin-top: 1px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 90%; }
</style>
</head>
<body>

<div class="topbar">
  <div class="logo">📅 Cal</div>
  <button class="today-btn" onclick="goToday()">Oggi</button>
  <div class="nav-btns">
    <button class="nav-btn" onclick="navigate(-1)">&#8249;</button>
    <button class="nav-btn" onclick="navigate(1)">&#8250;</button>
  </div>
  <div class="current-label" id="currentLabel"></div>
  <div class="spacer"></div>
  <div class="view-switcher">
    <button class="view-btn" data-view="day" onclick="setView('day')">Giorno</button>
    <button class="view-btn active" data-view="week" onclick="setView('week')">Settimana</button>
    <button class="view-btn" data-view="month" onclick="setView('month')">Mese</button>
  </div>
  <button class="chat-toggle-btn" onclick="toggleChat()">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
      <path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z"/>
    </svg>
    AI
  </button>
</div>

<div class="main">
  <div class="sidebar">
    <div class="mini-cal-header">
      <div class="mini-cal-title" id="miniTitle"></div>
      <div class="mini-nav">
        <button onclick="miniNav(-1)">&#8249;</button>
        <button onclick="miniNav(1)">&#8250;</button>
      </div>
    </div>
    <div class="mini-grid" id="miniGrid"></div>
  </div>

  <div class="cal-content" id="calContent"></div>

  <div class="chat-panel" id="chatPanel">
    <div class="chat-header">
      <div class="chat-header-top">
        <div class="chat-title">✨ Assistente</div>
        <button class="chat-close" onclick="toggleChat()">✕</button>
      </div>
      <div class="chat-subtitle">Dimmi cosa aggiungere o modificare.</div>
    </div>
    <div class="chat-messages" id="chatMessages">
      <div class="chat-msg ai">
        <div class="msg-avatar">✨</div>
        <div class="msg-bubble">Ciao! Dimmi cosa vuoi aggiungere.<br><br>Esempi:<br>• <em>"Dentista giovedì alle 14"</em><br>• <em>"Palestra ogni lun mer ven 10:30–12"</em><br>• <em>"Lavoro ogni lunedì 9–18 per 4 settimane"</em><br>• <em>"Elimina tutta la palestra ricorrente"</em></div>
      </div>
    </div>
    <div class="chat-input-area">
      <div class="chat-input-row">
        <textarea class="chat-textarea" id="chatInput" placeholder="Scrivi un comando…" rows="1"
          onkeydown="handleChatKey(event)" oninput="autoResize(this)"></textarea>
        <button class="chat-send" id="chatSend" onclick="sendChat()">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/>
          </svg>
        </button>
      </div>
      <div class="chat-hint">Invio per inviare · Shift+Invio per andare a capo</div>
    </div>
  </div>
</div>

<button class="fab" onclick="openModal()">+</button>

<div class="modal-overlay" id="modalOverlay" onclick="closeModalOutside(event)">
  <div class="modal">
    <h3 id="modalTitle">Nuovo evento</h3>
    <div class="form-group"><label>Titolo</label><input type="text" id="evTitle" placeholder="Es. Lavoro, Dentista…"></div>
    <div class="form-group"><label>Data</label><input type="date" id="evDate"></div>
    <div class="form-group">
      <label>Orario</label>
      <div class="time-row-form"><input type="time" id="evStart" style="flex:1"><span>→</span><input type="time" id="evEnd" style="flex:1"></div>
    </div>
    <div class="form-group"><label>Colore</label><div class="color-picker" id="colorPicker"></div></div>
    <div class="form-group"><label>Note</label><textarea id="evNote" placeholder="Facoltativo…"></textarea></div>
    <div class="modal-actions">
      <button class="btn-delete" id="btnDelete" style="display:none" onclick="deleteEvent()">Elimina</button>
      <button class="btn-cancel" onclick="closeModal()">Annulla</button>
      <button class="btn-save" onclick="saveEvent()">Salva</button>
    </div>
  </div>
</div>

<script>
const TODAY=new Date();TODAY.setHours(0,0,0,0);
let view='week',cursor=new Date(TODAY),miniCursor=new Date(TODAY);
let events=JSON.parse(localStorage.getItem('calEvents')||'[]'),editingId=null,chatOpen=false;
const EVENT_COLORS=['#3b5bfc','#f03c78','#00b894','#e17055','#fdcb6e','#6c5ce7','#0984e3'];
let selectedColor=EVENT_COLORS[0];
const DAYS_IT=['Dom','Lun','Mar','Mer','Gio','Ven','Sab'];
const MONTHS_IT=['Gennaio','Febbraio','Marzo','Aprile','Maggio','Giugno','Luglio','Agosto','Settembre','Ottobre','Novembre','Dicembre'];
const HOUR_PX=60,TOTAL_H=24*HOUR_PX;

function dateKey(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;}
function pad2(n){return String(n).padStart(2,'0');}
function timeToY(t){const[h,m]=t.split(':').map(Number);return(h+m/60)*HOUR_PX;}
function isToday(d){return dateKey(d)===dateKey(TODAY);}
function getWeekStart(d){const day=d.getDay(),diff=day===0?-6:1-day,s=new Date(d);s.setDate(d.getDate()+diff);s.setHours(0,0,0,0);return s;}
function eventsForDay(k){return events.filter(e=>e.date===k);}
function saveToStorage(){localStorage.setItem('calEvents',JSON.stringify(events));}

function goToday(){cursor=new Date(TODAY);render();}
function navigate(dir){
  if(view==='day')cursor.setDate(cursor.getDate()+dir);
  else if(view==='week')cursor.setDate(cursor.getDate()+dir*7);
  else cursor.setMonth(cursor.getMonth()+dir);
  render();
}
function miniNav(dir){miniCursor.setMonth(miniCursor.getMonth()+dir);renderMini();}
function setView(v){view=v;document.querySelectorAll('.view-btn').forEach(b=>b.classList.toggle('active',b.dataset.view===v));render();}

// ── HOLIDAYS ─────────────────────────────────────────────────────────────
function easterDate(year){
  // Anonymous Gregorian algorithm
  const a=year%19,b=Math.floor(year/100),c=year%100;
  const d=Math.floor(b/4),e=b%4,f=Math.floor((b+8)/25);
  const g=Math.floor((b-f+1)/3),h=(19*a+b-d-g+15)%30;
  const i=Math.floor(c/4),k=c%4,l=(32+2*e+2*i-h-k)%7;
  const m=Math.floor((a+11*h+22*l)/451);
  const month=Math.floor((h+l-7*m+114)/31);
  const day=((h+l-7*m+114)%31)+1;
  return new Date(year,month-1,day);
}

function getHoliday(dateStr){
  const [y,m,d]=dateStr.split('-').map(Number);
  const fixed={
    '01-01':'Capodanno','01-06':'Epifania','04-25':'Festa della Liberazione',
    '05-01':'Festa dei Lavoratori','06-02':'Festa della Repubblica',
    '08-15':'Ferragosto','11-01':'Ognissanti','12-08':'Immacolata Concezione',
    '12-25':'Natale','12-26':'Santo Stefano'
  };
  const key=`${String(m).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
  if(fixed[key])return fixed[key];
  // Easter & Easter Monday
  const easter=easterDate(y);
  const easterMonday=new Date(easter);easterMonday.setDate(easter.getDate()+1);
  if(dateStr===dateKey(easter))return 'Pasqua';
  if(dateStr===dateKey(easterMonday))return "Lunedì dell'Angelo";
  return null;
}

// ── CHAT ────────────────────────────────────────────────────────────────
function toggleChat(){
  chatOpen=!chatOpen;
  document.getElementById('chatPanel').classList.toggle('open',chatOpen);
  if(chatOpen)setTimeout(()=>document.getElementById('chatInput').focus(),300);
}
function autoResize(el){el.style.height='auto';el.style.height=Math.min(el.scrollHeight,100)+'px';}
function handleChatKey(e){if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat();}}

function addMsg(role,html){
  const msgs=document.getElementById('chatMessages');
  const div=document.createElement('div');div.className=`chat-msg ${role}`;
  div.innerHTML=`<div class="msg-avatar">${role==='ai'?'✨':'👤'}</div><div class="msg-bubble">${html}</div>`;
  msgs.appendChild(div);msgs.scrollTop=msgs.scrollHeight;return div;
}
function addTyping(){
  const msgs=document.getElementById('chatMessages');
  const div=document.createElement('div');div.className='chat-msg ai';div.id='typingIndicator';
  div.innerHTML=`<div class="msg-avatar">✨</div><div class="msg-bubble"><div class="typing-dots"><span></span><span></span><span></span></div></div>`;
  msgs.appendChild(div);msgs.scrollTop=msgs.scrollHeight;
}
function removeTyping(){const el=document.getElementById('typingIndicator');if(el)el.remove();}

// Expand a recurring event template into individual event instances
function expandRecurring(ev, recurDays, weeks){
  // recurDays: JS day numbers (0=Sun,1=Mon,2=Tue,3=Wed,4=Thu,5=Fri,6=Sat)
  // weeks: how many weeks to generate (default 8)
  const count = weeks || 8;
  const result = [];
  const groupId = Date.now().toString(36) + Math.random().toString(36).slice(2);

  // Start from the Monday of the current week
  const ws = getWeekStart(new Date(TODAY)); // always a Monday

  for(let w=0; w<count; w++){
    recurDays.forEach(dayNum=>{
      // Convert JS day (0=Sun…6=Sat) to offset from Monday (Mon=0…Sun=6)
      // Mon=1→0, Tue=2→1, Wed=3→2, Thu=4→3, Fri=5→4, Sat=6→5, Sun=0→6
      const offsetFromMonday = dayNum === 0 ? 6 : dayNum - 1;
      const d = new Date(ws);
      d.setDate(ws.getDate() + w * 7 + offsetFromMonday);
      result.push({
        id: Date.now().toString()+Math.random().toString(36).slice(2),
        title: ev.title,
        date: dateKey(d),
        startTime: ev.startTime,
        endTime: ev.endTime,
        color: ev.color||'#3b5bfc',
        note: ev.note||'',
        groupId,
      });
    });
  }
  return result;
}

async function sendChat(){
  const input=document.getElementById('chatInput');
  const text=input.value.trim();if(!text)return;
  input.value='';input.style.height='auto';
  document.getElementById('chatSend').disabled=true;
  addMsg('user',text);addTyping();

  const todayStr=dateKey(TODAY);
  const dayName=['Domenica','Lunedì','Martedì','Mercoledì','Giovedì','Venerdì','Sabato'][TODAY.getDay()];
  const evList=events.length?events.map(e=>`id:${e.id} | ${e.title} | ${e.date} | ${e.startTime}-${e.endTime}${e.groupId?' [ricorrente groupId:'+e.groupId+']':''}`).join('\n'):'(nessun evento)';

  const systemPrompt=`Sei un assistente calendario. Oggi è ${dayName} ${todayStr}.
Calcola sempre le date esatte YYYY-MM-DD a partire da oggi. La settimana inizia lunedì.

EVENTI ATTUALI:
${evList}

Rispondi SOLO con JSON valido (zero testo extra, zero markdown):
{
  "action": "add"|"delete"|"delete_group"|"modify"|"none",
  "reply": "breve risposta in italiano",
  "events": [{
    "id": "(solo per delete/modify)",
    "groupId": "(solo per delete_group: elimina tutte le occorrenze di un evento ricorrente)",
    "title": "...",
    "date": "YYYY-MM-DD",
    "startTime": "HH:MM",
    "endTime": "HH:MM",
    "color": "#3b5bfc",
    "note": "",
    "recurrence": {
      "days": [1,3,5],
      "weeks": 8
    }
  }]
}

REGOLE RICORRENZA:
- Se l'utente dice "ogni lunedì mercoledì venerdì" o simili → usa "recurrence" con days[] usando numeri JS: 0=Dom,1=Lun,2=Mar,3=Mer,4=Gio,5=Ven,6=Sab
- "weeks" = per quante settimane creare le occorrenze (default 8, max 52)
- Per eventi NON ricorrenti NON includere "recurrence"
- Per eliminare TUTTE le occorrenze di un evento ricorrente → action:"delete_group", metti groupId nell'evento

Colori: lavoro→#3b5bfc, medico→#f03c78, sport→#00b894, studio→#6c5ce7, cibo→#e17055, altro→#3b5bfc.
Per none: events=[].`;

  try{
    const res=await fetch('https://api.anthropic.com/v1/messages',{
      method:'POST',headers:{'Content-Type':'application/json'},
      body:JSON.stringify({model:'claude-sonnet-4-5',max_tokens:1000,system:systemPrompt,messages:[{role:'user',content:text}]})
    });
    const data=await res.json();
    const raw=data.content?.map(c=>c.text||'').join('').trim();
    if(!raw||data.error){addMsg('ai','API error: '+JSON.stringify(data.error||data));removeTyping();document.getElementById('chatSend').disabled=false;return;}

    let parsed;
    try{
      parsed=JSON.parse(raw.replace(/```json|```/g,'').trim());
    }catch{
      removeTyping();
      addMsg('ai', raw||'Non ho capito, riprova.');
      document.getElementById('chatSend').disabled=false;
      document.getElementById('chatInput').focus();
      return;
    }

    removeTyping();

    if(parsed.action==='add'&&parsed.events?.length){
      let firstDate=null;
      parsed.events.forEach(ev=>{
        if(ev.recurrence){
          const instances=expandRecurring(ev, ev.recurrence.days, ev.recurrence.weeks);
          instances.forEach(i=>events.push(i));
          if(!firstDate&&instances[0])firstDate=instances[0].date;
        } else {
          events.push({id:Date.now().toString()+Math.random().toString(36).slice(2),title:ev.title,date:ev.date,startTime:ev.startTime,endTime:ev.endTime,color:ev.color||'#3b5bfc',note:ev.note||''});
          if(!firstDate)firstDate=ev.date;
        }
      });
      saveToStorage();
      if(firstDate){const[y,m,d]=firstDate.split('-').map(Number);cursor=new Date(y,m-1,d);}
      render();
    } else if(parsed.action==='delete'&&parsed.events?.length){
      const ids=parsed.events.map(e=>e.id).filter(Boolean);
      events=events.filter(e=>!ids.includes(e.id));
      saveToStorage();render();
    } else if(parsed.action==='delete_group'&&parsed.events?.length){
      const gids=parsed.events.map(e=>e.groupId).filter(Boolean);
      events=events.filter(e=>!gids.includes(e.groupId));
      saveToStorage();render();
    } else if(parsed.action==='modify'&&parsed.events?.length){
      parsed.events.forEach(ev=>{const idx=events.findIndex(e=>e.id===ev.id);if(idx!==-1)events[idx]={...events[idx],...ev};});
      saveToStorage();render();
    }
    addMsg('ai', parsed.reply||'Fatto!');
  }catch(err){
    clearTimeout(timeout);
    removeTyping();
    addMsg('ai','Errore: '+err.message);
  }
  document.getElementById('chatSend').disabled=false;
  document.getElementById('chatInput').focus();
}

// ── LABEL ────────────────────────────────────────────────────────────────
function updateLabel(){
  const el=document.getElementById('currentLabel');
  if(view==='month'){el.textContent=`${MONTHS_IT[cursor.getMonth()]} ${cursor.getFullYear()}`;}
  else if(view==='week'){
    const ws=getWeekStart(cursor),we=new Date(ws);we.setDate(ws.getDate()+6);
    if(ws.getMonth()===we.getMonth())el.textContent=`${ws.getDate()}–${we.getDate()} ${MONTHS_IT[ws.getMonth()]} ${ws.getFullYear()}`;
    else el.textContent=`${ws.getDate()} ${MONTHS_IT[ws.getMonth()].slice(0,3)} – ${we.getDate()} ${MONTHS_IT[we.getMonth()].slice(0,3)} ${we.getFullYear()}`;
  }else{el.textContent=`${DAYS_IT[cursor.getDay()]} ${cursor.getDate()} ${MONTHS_IT[cursor.getMonth()]} ${cursor.getFullYear()}`;}
}

// ── MINI CAL ─────────────────────────────────────────────────────────────
function renderMini(){
  document.getElementById('miniTitle').textContent=`${MONTHS_IT[miniCursor.getMonth()].slice(0,3)} ${miniCursor.getFullYear()}`;
  const grid=document.getElementById('miniGrid');grid.innerHTML='';
  ['L','M','M','G','V','S','D'].forEach(d=>{const el=document.createElement('div');el.className='mini-day-name';el.textContent=d;grid.appendChild(el);});
  const first=new Date(miniCursor.getFullYear(),miniCursor.getMonth(),1);
  const last=new Date(miniCursor.getFullYear(),miniCursor.getMonth()+1,0);
  let off=(first.getDay()+6)%7;
  for(let i=off-1;i>=0;i--){const d=new Date(first);d.setDate(d.getDate()-i-1);grid.appendChild(makeMiniDay(d,true));}
  for(let i=1;i<=last.getDate();i++)grid.appendChild(makeMiniDay(new Date(miniCursor.getFullYear(),miniCursor.getMonth(),i),false));
  const total=off+last.getDate(),rem=total%7===0?0:7-total%7;
  for(let i=1;i<=rem;i++){const d=new Date(last);d.setDate(last.getDate()+i);grid.appendChild(makeMiniDay(d,true));}
}
function makeMiniDay(d,other){
  const el=document.createElement('div');el.className='mini-day';
  if(other)el.classList.add('other-month');
  if(isToday(d))el.classList.add('is-today');
  if(view==='week'){const ws=getWeekStart(cursor),we=new Date(ws);we.setDate(ws.getDate()+6);if(d>=ws&&d<=we&&!isToday(d))el.classList.add('in-view-week');}
  el.textContent=d.getDate();
  el.onclick=()=>{cursor=new Date(d);render();};
  return el;
}

// ── GRID HELPERS ─────────────────────────────────────────────────────────
function buildTimeCol(cls){
  const col=document.createElement('div');col.className=cls;col.style.cssText=`position:relative;height:${TOTAL_H}px`;
  for(let h=0;h<24;h++){const l=document.createElement('div');l.className='time-label';l.style.top=h*HOUR_PX+'px';l.textContent=h===0?'':`${pad2(h)}:00`;col.appendChild(l);}
  return col;
}
function addHourLines(col){
  for(let h=0;h<24;h++){
    const l=document.createElement('div');l.className='hour-line';l.style.top=h*HOUR_PX+'px';col.appendChild(l);
    const half=document.createElement('div');half.className='hour-line half';half.style.top=(h*HOUR_PX+HOUR_PX/2)+'px';col.appendChild(half);
  }
}
function addCurrentTimeLine(col){
  const nowY=()=>{const n=new Date();return(n.getHours()+n.getMinutes()/60)*HOUR_PX;};
  const line=document.createElement('div');line.className='current-time-line';line.style.top=nowY()+'px';col.appendChild(line);
  setInterval(()=>line.style.top=nowY()+'px',60000);
}
function renderEventBlock(ev,container,fullWidth=false){
  const top=timeToY(ev.startTime),bot=timeToY(ev.endTime),h=Math.max(bot-top,18);
  const el=document.createElement('div');el.className='cal-event';
  el.style.cssText=`top:${top}px;height:${h}px;background:${ev.color};`;
  if(fullWidth){el.style.left='4px';el.style.right='4px';}
  el.innerHTML=`<div class="ev-title">${ev.title}</div>${h>28?`<div class="ev-time">${ev.startTime}–${ev.endTime}</div>`:''}`;
  el.onclick=e=>{e.stopPropagation();openModal(ev);};
  container.appendChild(el);
}

// ── WEEK ─────────────────────────────────────────────────────────────────
function renderWeek(){
  const ws=getWeekStart(cursor);
  const wrap=document.createElement('div');wrap.className='week-view';
  const hdr=document.createElement('div');hdr.className='week-header';
  const g=document.createElement('div');g.className='week-header-gutter';hdr.appendChild(g);
  for(let i=0;i<7;i++){
    const d=new Date(ws);d.setDate(ws.getDate()+i);
    const day=document.createElement('div');day.className='week-header-day';day.onclick=()=>{cursor=new Date(d);setView('day');};
    const name=document.createElement('div');name.className='whd-name';name.textContent=['Lun','Mar','Mer','Gio','Ven','Sab','Dom'][i];
    const num=document.createElement('div');num.className='whd-num';if(isToday(d))num.classList.add('is-today');if(i>=5)num.classList.add('is-weekend');
    num.textContent=d.getDate();day.appendChild(name);day.appendChild(num);
    const hol=getHoliday(dateKey(d));if(hol){const hb=document.createElement('div');hb.className='whd-holiday';hb.textContent=hol;hb.title=hol;day.appendChild(hb);}
    hdr.appendChild(day);
  }
  wrap.appendChild(hdr);
  const body=document.createElement('div');body.className='week-body';
  body.appendChild(buildTimeCol('week-time-col'));
  const grid=document.createElement('div');grid.className='week-days-grid';
  for(let i=0;i<7;i++){
    const d=new Date(ws);d.setDate(ws.getDate()+i);
    const col=document.createElement('div');col.className='week-day-col';if(i>=5)col.classList.add('is-weekend');col.style.height=TOTAL_H+'px';
    addHourLines(col);if(isToday(d))addCurrentTimeLine(col);
    eventsForDay(dateKey(d)).forEach(ev=>renderEventBlock(ev,col));
    col.ondblclick=e=>{const r=col.getBoundingClientRect();prefillModal(d,Math.floor((e.clientY-r.top)/HOUR_PX));};
    grid.appendChild(col);
  }
  body.appendChild(grid);wrap.appendChild(body);
  setTimeout(()=>body.scrollTop=7*HOUR_PX-20,10);return wrap;
}

// ── MONTH ────────────────────────────────────────────────────────────────
function renderMonth(){
  const wrap=document.createElement('div');wrap.className='month-view';
  const hdr=document.createElement('div');hdr.className='month-header-row';
  ['Lun','Mar','Mer','Gio','Ven','Sab','Dom'].forEach(d=>{const el=document.createElement('div');el.className='month-day-name';el.textContent=d;hdr.appendChild(el);});
  wrap.appendChild(hdr);
  const grid=document.createElement('div');grid.className='month-grid';
  const first=new Date(cursor.getFullYear(),cursor.getMonth(),1),last=new Date(cursor.getFullYear(),cursor.getMonth()+1,0);
  let off=(first.getDay()+6)%7,cells=[];
  for(let i=off-1;i>=0;i--){const d=new Date(first);d.setDate(d.getDate()-i-1);cells.push({d,other:true});}
  for(let i=1;i<=last.getDate();i++)cells.push({d:new Date(cursor.getFullYear(),cursor.getMonth(),i),other:false});
  while(cells.length<42){const p=cells[cells.length-1].d,d=new Date(p);d.setDate(p.getDate()+1);cells.push({d,other:true});}
  cells.forEach(({d,other})=>{
    const cell=document.createElement('div');cell.className='month-cell';
    cell.ondblclick=()=>{cursor=new Date(d);openModal();};cell.onclick=()=>{cursor=new Date(d);setView('day');};
    const num=document.createElement('div');num.className='month-cell-num';
    if(other)num.classList.add('other');if(isToday(d))num.classList.add('today');
    if(!other&&(d.getDay()===0||d.getDay()===6))num.classList.add('is-weekend');
    num.textContent=d.getDate();cell.appendChild(num);
    const mhol=getHoliday(dateKey(d));if(mhol&&!other){const hb=document.createElement('div');hb.className='holiday-badge';hb.textContent=mhol;hb.title=mhol;cell.appendChild(hb);}
    eventsForDay(dateKey(d)).slice(0,3).forEach(ev=>{
      const el=document.createElement('div');el.className='month-event';el.style.background=ev.color;el.textContent=ev.title;
      el.onclick=e=>{e.stopPropagation();openModal(ev);};cell.appendChild(el);
    });
    grid.appendChild(cell);
  });
  wrap.appendChild(grid);return wrap;
}

// ── DAY ──────────────────────────────────────────────────────────────────
function renderDay(){
  const wrap=document.createElement('div');wrap.className='day-view';
  const hdr=document.createElement('div');hdr.className='day-view-header';
  const name=document.createElement('div');name.className='day-view-name';
  name.textContent=['Domenica','Lunedì','Martedì','Mercoledì','Giovedì','Venerdì','Sabato'][cursor.getDay()];
  const num=document.createElement('div');num.className='day-view-num';num.style.color=isToday(cursor)?'var(--accent)':'var(--text)';
  num.textContent=cursor.getDate()+' '+MONTHS_IT[cursor.getMonth()];
  hdr.appendChild(name);hdr.appendChild(num);
  const dhol=getHoliday(dateKey(cursor));if(dhol){const hb=document.createElement('div');hb.className='day-holiday-banner';hb.textContent='🇮🇹 '+dhol;hdr.appendChild(hb);}
  wrap.appendChild(hdr);
  const body=document.createElement('div');body.className='day-view-body';
  body.appendChild(buildTimeCol('day-time-col'));
  const col=document.createElement('div');col.className='day-col-content';col.style.height=TOTAL_H+'px';
  addHourLines(col);if(isToday(cursor))addCurrentTimeLine(col);
  eventsForDay(dateKey(cursor)).forEach(ev=>renderEventBlock(ev,col,true));
  col.ondblclick=e=>{const r=col.getBoundingClientRect();prefillModal(cursor,Math.floor((e.clientY-r.top)/HOUR_PX));};
  body.appendChild(col);wrap.appendChild(body);
  setTimeout(()=>body.scrollTop=7*HOUR_PX-20,10);return wrap;
}

// ── RENDER ───────────────────────────────────────────────────────────────
function render(){
  updateLabel();renderMini();
  const c=document.getElementById('calContent');c.innerHTML='';
  if(view==='week')c.appendChild(renderWeek());
  else if(view==='month')c.appendChild(renderMonth());
  else c.appendChild(renderDay());
}

// ── MODAL ────────────────────────────────────────────────────────────────
function buildColorPicker(){
  const cp=document.getElementById('colorPicker');cp.innerHTML='';
  EVENT_COLORS.forEach(c=>{const dot=document.createElement('div');dot.className='color-dot'+(c===selectedColor?' selected':'');dot.style.background=c;dot.onclick=()=>{selectedColor=c;buildColorPicker();};cp.appendChild(dot);});
}
function openModal(ev=null){
  editingId=ev?ev.id:null;
  document.getElementById('modalTitle').textContent=ev?'Modifica evento':'Nuovo evento';
  document.getElementById('btnDelete').style.display=ev?'block':'none';
  if(ev){document.getElementById('evTitle').value=ev.title;document.getElementById('evDate').value=ev.date;document.getElementById('evStart').value=ev.startTime;document.getElementById('evEnd').value=ev.endTime;document.getElementById('evNote').value=ev.note||'';selectedColor=ev.color;}
  else{document.getElementById('evTitle').value='';document.getElementById('evDate').value=dateKey(cursor);document.getElementById('evStart').value='09:00';document.getElementById('evEnd').value='10:00';document.getElementById('evNote').value='';selectedColor=EVENT_COLORS[0];}
  buildColorPicker();document.getElementById('modalOverlay').classList.add('open');
  setTimeout(()=>document.getElementById('evTitle').focus(),150);
}
function prefillModal(d,h){
  editingId=null;document.getElementById('modalTitle').textContent='Nuovo evento';document.getElementById('btnDelete').style.display='none';
  document.getElementById('evTitle').value='';document.getElementById('evDate').value=dateKey(d);
  document.getElementById('evStart').value=`${pad2(h)}:00`;document.getElementById('evEnd').value=`${pad2(Math.min(h+1,23))}:00`;
  document.getElementById('evNote').value='';selectedColor=EVENT_COLORS[0];
  buildColorPicker();document.getElementById('modalOverlay').classList.add('open');
  setTimeout(()=>document.getElementById('evTitle').focus(),150);
}
function closeModal(){document.getElementById('modalOverlay').classList.remove('open');}
function closeModalOutside(e){if(e.target===document.getElementById('modalOverlay'))closeModal();}
function saveEvent(){
  const title=document.getElementById('evTitle').value.trim();if(!title){document.getElementById('evTitle').focus();return;}
  const ev={id:editingId||Date.now().toString(),title,date:document.getElementById('evDate').value,startTime:document.getElementById('evStart').value,endTime:document.getElementById('evEnd').value,note:document.getElementById('evNote').value,color:selectedColor};
  if(editingId){const idx=events.findIndex(e=>e.id===editingId);if(idx!==-1)events[idx]=ev;}else events.push(ev);
  saveToStorage();closeModal();render();
}
function deleteEvent(){if(!editingId)return;events=events.filter(e=>e.id!==editingId);saveToStorage();closeModal();render();}
document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal();});

render();
</script>
</body>
</html>
