{
  "name": "lavagna26-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LAVAGNA '26 — backend: aggrega API reali (ESPN, TheSportsDB, The Odds API, football-data) e calcola i voti di valore. Nessun dato fattuale generato da AI.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test:engine": "node src/engine.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
